import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["images.pexels.com", "media.istockphoto.com", "via.placeholder.com" , "data:image/jpeg" ,"cdn.sanity.io"],
  },
};

export default nextConfig;
